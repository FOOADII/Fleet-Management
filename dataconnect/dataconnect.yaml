specVersion: "v1"
serviceId: "fleetapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fleetapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
